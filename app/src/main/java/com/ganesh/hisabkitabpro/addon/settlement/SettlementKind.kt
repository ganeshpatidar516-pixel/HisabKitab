package com.ganesh.hisabkitabpro.addon.settlement

/** Optional classification on a transaction row — null = legacy / unspecified. */
object SettlementKind {
    const val PARTIAL = "PARTIAL"
    const val FULL = "FULL"
}
